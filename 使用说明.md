# AI 同声传译助手 - 使用说明

适用于业务同事的安装与使用指南。本文档场景为：**单通道模式 + 不输出语音 + USB 声卡输入 + 投屏显示翻译字幕**。

---

## 1. 功能概述

```
USB 声卡输入 → 浏览器采集音频 → 豆包同声传译大模型 → 实时字幕返回浏览器
→ 主界面显示 + 投影屏同步展示
```

本工具通过浏览器采集 USB 声卡输入的音频，调用豆包同声传译大模型进行实时翻译，将译文和原文同时显示在主界面和独立的投影屏窗口上，适合会议、演讲等需要投屏展示翻译字幕的场景。

---

## 2. 环境准备

### 硬件
- **电脑**（Windows 10/11）
- **USB 声卡转换器**（已准备，连接麦克风/音响）
- **麦克风**（接在 USB 声卡上，用于拾取发言）
- **投影设备**（投影仪/大屏显示器，HDMI 或无线投屏）

### 网络
- 电脑已连接网络（Wi-Fi 或有线）
### 软件
- **Chrome 或 Edge 浏览器**（推荐 Chrome）

---

## 3. 安装与启动

1. 在项目目录（含 `install.ps1` 的文件夹）下，**右键点击空白处** → 选择 **"在终端中打开"**（或打开 PowerShell，`cd` 到项目目录）

2. 在 PowerShell 中执行以下命令：

   ```powershell
   .\install.ps1
   ```

3. 脚本会自动完成以下操作：
   - 检查是否安装了 Python，如未安装则自动下载安装
   - 检查是否安装了 pip，如未安装则自动安装
   - 安装项目所需的依赖包
   - 启动服务

4. 浏览器打开 `http://localhost:5004`，页面**右下角显示「服务已连接」** 表示启动成功。

> 💡 如果浏览器没有自动弹出，请手动打开 Chrome/Edge，在地址栏输入 `http://localhost:5004`。

---

## 4. 使用步骤

请按以下顺序操作：

### 步骤 1：连接 USB 声卡
将 USB 声卡转换器插入电脑 USB 接口，确保系统已识别该设备。

### 步骤 2：打开配置页面
在 Chrome 中访问 `http://localhost:5004`，你将看到四列配置界面。

### 步骤 3：设置语言方向
在 **"语言方向 / Language"** 区域设置：
- **My Speak（我说的）** → 选择 **English**
- **Their Hear（对方听到的）** → 选择 **中文 Chinese**

即：你说英文，投影屏显示中文翻译。

![语言方向](docs/images/usage-lang-direction.png)

### 步骤 4：确认通道模式
在 **"本地音频 / Audio Devices"** 区域，确认"通道模式 / Channel Mode"已选择：
> **「单通道 / Single (仅麦克风输入)」**（默认即为此选项，无需更改）

![单通道模式](docs/images/usage-select-channel.png)

### 步骤 5：选择麦克风
在 **"本地音频 / Audio Devices"** 区域，确保麦克风选择了你的 **USB 声卡设备**（通常设备名称会包含 USB 字样）。

![选择麦克风](docs/images/usage-select-mic.png)

### 步骤 6：关闭语音输出
在同一区域，**取消勾选**「启用语音播放 / Enable TTS」（我们只需要文字翻译，不需要语音播放，且可以节省 API 费用）。

![关闭TTS](docs/images/usage-disable-tts.png)

### 步骤 7：（可选）选择术语库
在 **"术语库 / Glossary"** 区域，勾选需要使用的术语分类。术语可以提升特定领域词汇的翻译准确性。
如果没有术语库，请先点击顶部导航栏的 **「术语管理 / Glossary Mgr」**，在里面新建术语分类并添加术语。

![术语库](docs/images/usage-glossary.png)

### 步骤 8：设置投影屏样式
在 **"显示设置 / Display"** 区域，根据投屏环境调整以下参数（便于远距离观看）：

| 参数 | 说明 | 建议值 |
|---|---|---|
| 字号 / Font Size | 投影屏上文字大小 | 24px 以上 |
| 行高 / Line Height | 文字行间距 | 1.6 |
| 译文颜色 / Trans Color | 译文文字颜色 | 白色 #ffffff |
| 原文颜色 / Orig Color | 原文文字颜色 | 浅灰 #b3b3b3 |
| 背景颜色 / Background | 投影屏背景色 | 黑色 #000000 |
| 背景透明度 / Opacity | 背景不透明度 | 100%（不透明） |

> 💡 可以点击「预览 / Preview」按钮先查看效果。

![显示设置](docs/images/usage-display.png)

### 步骤 9：智能校准
点击 **⚡ 智能校准 / Auto Calibrate** 按钮，**保持安静 3 秒钟**。系统会自动测量当前环境的底噪，设定合适的语音检测阈值。

### 步骤 10：开始运行
点击底部右下角的 **「开始运行 / Start」** 按钮，进入翻译运行界面。

### 步骤 11：打开投影屏
在运行界面的顶部工具栏中，点击 **📺 打开投影屏** 按钮，会弹出一个独立的投影窗口。

### 步骤 12：投屏显示
将弹出的投影窗口**拖动到投影显示器**上，按 **F11** 全屏显示。窗口分上下两部分：
- 上半部分：**译文 / Translation**（观众看到的翻译结果）
- 下半部分：**原文 / Original**（你的原始发言）

![投影屏](docs/images/usage-projection.png)

### 步骤 13：开始翻译
对准麦克风正常发言，投影屏上会实时显示翻译字幕。

### 步骤 14：结束运行
使用完毕后，点击运行界面右上角的 **✕** 按钮结束当前会话。

### 步骤 15：（可选）查看会议记录
点击顶部导航栏的 **「会议记录 / Meetings」**，可以查看所有历史会话的翻译记录，支持：
- 查看详细的双语对照内容
- 导出为 **TXT** 或 **Markdown** 格式

![会议记录](docs/images/usage-meetings.png)

---

## 5. 投影屏设置详解

在运行前，可在"显示设置"区域调整以下参数，设置会同步应用到投影屏窗口：

| 参数 | 范围 | 说明 |
|---|---|---|
| 字号 | 10px ~ 72px | 越大越适合远距离观看 |
| 行高 | 1.0 ~ 3.0 | 适当增大行高可提升可读性 |
| 译文颜色 | 任意颜色 | 建议与背景色保持高对比度 |
| 原文颜色 | 任意颜色 | 建议略暗于译文颜色 |
| 背景颜色 | 任意颜色 | 投屏场景建议深色背景 |
| 背景透明度 | 0% ~ 100% | 0% 完全透明，100% 完全不透明 |

> 投影屏窗口打开后，可在配置页面调整参数后点击**预览**查看效果，新设置会实时同步到投影屏。

---

## 6. 注意事项

### 浏览器麦克风权限
- 首次访问时，浏览器会弹出麦克风权限请求，请点击**允许**
- 如果误点了拒绝，点击地址栏左侧的 🔒 或**摄像头图标**，重新开启麦克风权限
- 本地运行必须使用 `http://127.0.0.1` 或 `http://localhost` 访问，否则浏览器会禁止非 HTTPS 下使用麦克风

### USB 声卡连接顺序
- **建议先连接 USB 声卡，再打开配置页面**，这样设备才会出现在麦克风下拉列表中
- 如果插上声卡后页面未识别，刷新页面即可

### 网络要求
- 电脑必须联网，且能正常访问豆包同声传译 API

### 使用结束后
- 使用完毕后点击运行界面右上角的 **✕** 按钮正确结束会话，否则翻译记录可能不完整

---

## 7. 常见问题 FAQ

### Q：页面右下角一直显示"连接中... / Connecting..."怎么办？
A：检查网络是否正常，刷新页面重试。如果仍不行，检查 PowerShell 窗口是否有报错信息，截图发给技术支持。

### Q：麦克风下拉菜单是空的
A：说明浏览器未检测到麦克风设备。请确认：
1. USB 声卡已正确插入电脑
2. 刷新页面重试
3. 检查系统声音设置中是否识别到该设备

### Q：点击开始运行后，说话没有翻译反应
A：请按顺序排查：
1. 是否已点击 **⚡ 智能校准**
2. 是否已允许浏览器麦克风权限
3. 麦克风是否选择了正确的 USB 声卡设备
4. 校准说话声音是否足够大（可以重新校准）

### Q：投影屏窗口不小心关闭了
A：在运行界面顶部工具栏中再次点击 **📺 打开投影屏** 即可重新打开。

### Q：如何查看之前会议的历史翻译记录？
A：点击页面顶部导航栏的 **「会议记录 / Meetings」**，可以查看所有历史会话，并支持导出为 TXT 或 Markdown 文件。

### Q：右下角状态显示"连接AI服务失败：python-socks is required to use a socks proxy"
A：这是因为电脑上开启了网络代理工具（如 Clash、Surge、Shadowsocks 等），`websockets` 库检测到系统 SOCKS 代理设置但缺少依赖包导致。

解决方法：
1. **临时方案**：关闭代理工具的系统代理功能，或退出代理软件，然后刷新页面重新运行
2. **替代方案**：在代理工具的「绕过/忽略」列表中添加 `openspeech.bytedance.com`，让翻译服务的连接绕过代理

---

*如有其他问题，请联系技术支持。*
